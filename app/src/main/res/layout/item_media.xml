<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="@dimen/media_image_width"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/imageViewMedia"
        android:layout_width="@dimen/media_image_width"
        android:layout_height="@dimen/media_image_height"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_regular"
        android:textAppearance="@style/TextStyle.17.Bold"
        android:textColor="@color/white"
        tools:text="The Cure For Loneliness" />

    <TextView
        android:id="@+id/textViewSubtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_regular"
        android:textAllCaps="true"
        android:textAppearance="@style/TextStyle.13.Bold"
        android:textColor="@color/gray"
        tools:text="Mindvalley Mentoring" />

</LinearLayout>